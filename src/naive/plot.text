# Run as "gnuplot -e "n=#P" plot.text
# #P = number of particles in the simulation

reset

set terminal gif animate
set output 'test.gif'
set xrange[-2:2]
set yrange[-2:2]
unset key
do for [i=1:300] {
    plot  'output.dat' every ::n*(i-1)+1::n*i 
}
